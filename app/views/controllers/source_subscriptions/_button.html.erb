<%= turbo_frame_tag dom_id(source, folder.id) do %>
  <%= component(:button,
    size: :sm,
    variant: folder.subscribe?(source) ? :destructive : :outline,
    href: source_source_subscriptions_path(source, folder_id: folder.id),
    data: { turbo_method: :post }
  ) { folder.subscribe?(source) ? "解除" : "フォロー" } %>
<% end %>
